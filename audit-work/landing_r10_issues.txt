

# Security Audit Report: install_landing_v3.36_patched.sh

## CRITICAL Issues

### [1] Category: systemctl failures Severity: CRITICAL Line: 757
Description: `systemctl enable` lacks error checking. If enable fails, service won't survive reboot, but script continues and may report success. With `set -e`, this causes unclean exit without rollback.
Fix: (unified diff)
```diff
--- a/script
+++ b/script
@@ -754,7 +754,8 @@
   write_logrotate
   # [F5] daemon-reload must succeed for unit changes to take effect
   systemctl daemon-reload \
     || die "daemon-reload 失败，systemd 图未更新"
-  systemctl enable "$LANDING_SVC"
+  systemctl enable "$LANDING_SVC" \
+    || die "systemctl enable ${LANDING_SVC} 失败，服务无法开机自启"
   systemctl restart "$LANDING_SVC"
   sleep 2
```

### [2] Category: set -euo pipefail bypasses Severity: CRITICAL Line: 869
Description: iptables ICMP rate-limit rule uses `|| true`, silently hiding failures. If rule addition fails, ICMP flood protection is disabled but script continues. Inconsistent with IPv6